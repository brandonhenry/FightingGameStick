# Windows input host output

Run `pnpm build:host` on a machine with the .NET 8 SDK. The self-contained `FightingGameStick.InputHost.exe` is written here and bundled into Windows packages by Electron Forge.
